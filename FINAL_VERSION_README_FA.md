# Trading OS — Final QA Patch

این نسخه از آخرین نسخه سالم `Trading-OS-Final-QA-Fixed.zip` ساخته شده، نه از v2 خراب.

## تغییرات نهایی
- منطق ثبت/بسته‌شدن معامله نسخه سالم حفظ شده است.
- `exitPriceValue` که در نسخه قبلی به‌صورت ناقص باقی مانده بود اضافه شد؛ این خطا می‌توانست باعث شکست کامپایل صفحه ثبت معامله شود.
- دکمه «بستن معامله» مستقیماً به `/trade/{id}/edit?focus=exit` می‌رود.
- فرم ویرایش در حالت `focus=exit` بخش خروج را خودکار باز می‌کند، اسکرول می‌کند و روی قیمت خروج فوکوس می‌گذارد.
- منطق Timezone و آپلود/حذف عکس نسخه سالم حفظ شده است.
- تنظیمات Supabase/Database در این Patch تغییر نکرده‌اند.
- برای موبایل، تنظیمات LAN قبلی حفظ شده است.

## مهم
این نسخه نیاز به اجرای SQL جدید ندارد.
از `.env` نسخه‌ای که الان برایت کار می‌کند استفاده کن. فایل `.env.example` فقط نمونه است و نباید با مقادیر واقعی جایگزین شود.

## تست پیشنهادی
1. صفحه ثبت معامله را باز کن.
2. یک معامله تستی ثبت کن.
3. از صفحه جزئیات روی «بستن معامله» بزن.
4. باید مستقیم روی قیمت/زمان خروج بروی.
5. قیمت و ساعت خروج را ثبت کن و ذخیره بزن.
6. معامله باید `closed`/win/loss/breakeven شود و `is_open=false` بماند.
7. یک عکس آپلود، حذف و دوباره آپلود کن.

## Build note
در محیط این بررسی، نصب npm به دلیل نبود cache کامل وابستگی‌ها و timeout شبکه تکمیل نشد؛ بنابراین Build production واقعی در این محیط اجرا نشد. در عوض، فایل‌های تغییرکرده با TypeScript parser بررسی شدند و خطای محلی `exitPriceValue` که پیدا شد اصلاح شد.
