# Trading OS — Final Consolidated Build

مبنا: `Trading-OS-Final-QA-v5-Reports-History-News-Prop(1)` به‌عنوان نسخه پایه پایدار.

## فقط این تغییرات جدید به نسخه پایه اضافه شده‌اند

1. حساب اصلی/فعال مرکزی در داشبورد
- انتخاب حساب از Dashboard با عنوان «حساب اصلی تحلیل».
- انتخاب در localStorage ذخیره می‌شود.
- بخش‌های تحلیلی و حساب‌محور از همان account_id استفاده می‌کنند.
- ثبت معامله جدید همان حساب را به‌صورت پیش‌فرض انتخاب می‌کند.

2. حجم معامله
- حداقل 0.01 لات.
- گام دقیق 0.01: 0.01، 0.02، ...، 0.09، 0.10، 0.11، ...
- محاسبه خودکار حجم به نزدیک‌ترین/پایین‌ترین گام معتبر نرمال می‌شود تا ریسک بیش از مقدار هدف نشود.

3. دقت پولی و کمیسیون
- گرد کردن مقادیر پولی به دو رقم اعشار.
- نرخ کمیسیون از اولین معامله دارای کمیسیون واقعی برای همان نماد در همان حساب یاد گرفته می‌شود.
- اگر برای نماد سابقه نباشد، از اولین سابقه همان حساب استفاده می‌شود.
- معاملات بعدی به‌صورت خودکار کمیسیون را بر اساس نرخ هر لات محاسبه می‌کنند.

4. اخبار اقتصادی
- Failover سمت Supabase Edge Function، نه مرورگر کاربر.
- اولویت منابع: FairEconomy/Forex Factory mirror → CDN mirror → Forex Factory CSV → ClawFX calendar → ClawFX mirror → Trading Economics guest.
- اگر یک منبع خطا بدهد یا خبر معتبر امروز برنگرداند، منبع بعدی امتحان می‌شود.
- فقط خبرهای امروز به وقت Asia/Tehran، USD/EUR و High/Medium نمایش داده می‌شوند.
- صفحه در صورت قطع همه منابع Crash نمی‌کند و آخرین داده معتبر محلی را تا 30 دقیقه نگه می‌دارد.

5. گزارش‌ها و تاریخچه
- ساختار حل‌شده v5 دست‌نخورده حفظ شده است.
- `Reports.tsx` همچنان از `ReactNode` برای Row استفاده می‌کند.
- History همان CSS Grid همسان هدر/ردیف v5 را حفظ می‌کند و ستون‌ها زیر هم نمی‌ریزند.
- گزارش‌ها و تاریخچه هنگام تغییر حساب فعال دوباره بارگذاری می‌شوند.

## Supabase
- برای جداول/SQL تغییر جدیدی لازم نیست.
- برای اخبار باید فقط Edge Function `economic-calendar` نسخه جدید Deploy شود.

## Validation
- `tsc --noEmit -p tsconfig.json` موفق شد.
- Edge Function با TypeScript transpile syntax check شد.
- Production build کامل به‌دلیل محدودیت نصب پکیج در محیط QA اجرا نشد.
