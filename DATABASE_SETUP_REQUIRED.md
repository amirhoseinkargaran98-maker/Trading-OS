# نصب بسیار ساده Trading OS

1. `supabase/COMPLETE_DATABASE_SETUP.sql` را یک بار در **Supabase → SQL Editor → Run** اجرا کنید.
2. `.env` را با `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` تنظیم کنید.
3. پروژه را اجرا کنید.

برای Settings دیگر نیازی به اجرای فایل SQL جداگانه نیست؛ Master Database شامل ساختار و repair function لازم است.
