# اجرای Trading OS روی گوشی از طریق Wi‑Fi

این پروژه برای دسترسی از گوشی در همان شبکه محلی آماده شده است.

## 1) لپ‌تاپ و گوشی
هر دو باید به **یک Wi‑Fi/شبکه محلی** متصل باشند.

## 2) اجرای پروژه
در پوشه پروژه:

```bash
npm install
npm run dev
```

در خروجی Vite یک آدرس `Network` نمایش داده می‌شود؛ همان آدرس را روی گوشی باز کن، نه `localhost`. مثال:

```text
http://192.168.1.20:5173
```

## 3) اگر Network URL باز نشد
- Windows Firewall را برای Node.js/Vite روی شبکه Private اجازه بده.
- VPN/Proxy لپ‌تاپ یا گوشی را موقتاً خاموش کن یا مطمئن شو هر دو از یک شبکه عبور می‌کنند.
- از IP واقعی لپ‌تاپ استفاده کن، نه `127.0.0.1` یا `localhost`.
- اگر پورت 5173 اشغال باشد Vite خودش پورت بعدی را اعلام می‌کند؛ همان پورت را روی گوشی استفاده کن.

## نکته مهم
`localhost` روی گوشی یعنی **خود گوشی**، نه لپ‌تاپ. بنابراین `http://localhost:5173` روی گوشی به پروژه لپ‌تاپ وصل نمی‌شود.
